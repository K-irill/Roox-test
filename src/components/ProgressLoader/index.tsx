import * as React from "react";
import CircularProgress from "@mui/material/CircularProgress";

const ProgressLoader = () => {
  return <CircularProgress sx={{ mb: "10px" }} />;
};

export default ProgressLoader;
